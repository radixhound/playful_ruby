<%= turbo_stream.replace "keybindings", partial: "/action_intents/cancel" %>
<% # this is a really hacky way of clearing the command pallette %>
<% # ultimately we need partials in a central place to render the command pallettes %>